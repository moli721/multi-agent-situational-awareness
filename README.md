# Workspace Layout

The runnable project has been consolidated into:

- `app/` (backend + frontend + scripts)

Reference materials remain at root:

- `thesis/`
- `*.extracted.txt`

To run the project, go to:

```bash
cd app
```

Then follow:

- `app/README.md`
