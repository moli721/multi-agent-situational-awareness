# Frontend Lightweight i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为当前前端增加轻量级中英切换能力，并增加前后端端口提示，减少使用混淆。

**Architecture:** 新增一个无依赖的 `i18n.js` 字典/翻译工具模块，在 `App.jsx` 中接入 `language` 状态和 `t()` 调用，保留现有页面结构不变。

**Tech Stack:** React, Vite, Node test

---

### Task 1: Write failing tests for i18n helpers

**Files:**
- Create: `app/web/src/i18n.test.js`
- Create: `app/web/src/i18n.js`

- [ ] **Step 1: Write the failing tests**

```js
test("normalizeLanguage keeps only zh/en", ...)
test("resolveInitialLanguage defaults to zh", ...)
test("translate resolves keys and interpolates vars", ...)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `npm test`
Expected: FAIL because `i18n.js` does not exist yet.

### Task 2: Implement the i18n helper module

**Files:**
- Create: `app/web/src/i18n.js`
- Test: `app/web/src/i18n.test.js`

- [ ] **Step 1: Add dictionaries and helper functions**
- [ ] **Step 2: Run tests**

Run: `npm test`
Expected: PASS

### Task 3: Wire the language toggle into the page

**Files:**
- Modify: `app/web/src/App.jsx`
- Modify: `app/web/src/styles.css`

- [ ] **Step 1: Add language state and header toggle**
- [ ] **Step 2: Replace major visible UI strings with dictionary lookups**
- [ ] **Step 3: Add frontend/backend endpoint hint**

### Task 4: Verify the UI build

**Files:**
- Modify: `app/web/package.json` if needed

- [ ] **Step 1: Run frontend tests**

Run: `npm test`
Expected: PASS

- [ ] **Step 2: Run production build**

Run: `npm run build`
Expected: PASS
